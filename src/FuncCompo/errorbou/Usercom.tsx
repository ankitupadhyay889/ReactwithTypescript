import React from 'react';

const Usercom = () => {
    return (
        <div>
            <h1> Ankit </h1>
            {new Error("Error in user component")}
        </div>
    );
};

export default Usercom;